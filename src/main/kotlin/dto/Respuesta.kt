package dto

import com.google.gson.Gson
import com.google.gson.GsonBuilder
import util.toLocalMoney
import java.time.LocalDateTime
import java.util.*

@Deprecated("")
class Respuesta() {
    /*
    val id: UUID = UUID.randomUUID()
    var code: Int = 0
    lateinit var body: List<DTO>
    val generatedAt = LocalDateTime.now().toString()

    constructor(
        code: Int,
        body: List<DTO>
    ) : this() {
        this.code = code
        this.body = body
    }

    fun fromJSON(json: String): Respuesta? {
        return Gson().fromJson(json, Respuesta::class.java)
    }

    fun toJSON(): String {
        return GsonBuilder().setPrettyPrinting().create().toJson(this)
    }

    override fun toString(): String {
        return GsonBuilder().setPrettyPrinting().create().toJson(this)
    }

     */
}